%% sin datas generator
%{
    特定频率正弦信号发生器
%}
function [SinSg] = SinSingnal(length,fre,noise)
    fprintf('正弦信号发生器,长度%d\t频率%d ...\t噪声%d\n', ...
        length,fre,noise );
    SinSg = zeros(0);
    
    if isequal(noise,1)
    else
end